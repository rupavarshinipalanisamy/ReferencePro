import React from 'react';
import { Text, View } from 'react-native';

export type GroupInfoProps = {
    
    }


const GroupInfo = (props: GroupInfoProps) => {
    return (
        <View>
            <Text>GroupInfo component</Text>
        </View>
    )
}

export default GroupInfo