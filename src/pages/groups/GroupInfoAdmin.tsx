import React from 'react';
import { Text, View } from 'react-native';

export type GroupInfoAdminProps = {
    
    }


const GroupInfoAdmin = (props: GroupInfoAdminProps) => {
    return (
        <View>
            <Text>GroupInfoAdmin component</Text>
        </View>
    )
}

export default GroupInfoAdmin