import { labels } from "../labels";

export const manageDevicesdata =[
    {
        id:1,
        date:labels.dateText,
        device:labels.device,
        ipAdd:labels.ipAdd,
        location:labels.location,
        status:labels.status,
        dateAndtime:labels.dateAndtime,
        deviceName:labels.devicename,
        ipId:labels.ipAddId,
        devLoc:labels.deviceArea
        

    }
]