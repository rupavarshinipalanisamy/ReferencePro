import React from 'react';
import { Text, View } from 'react-native';

export type passwordProps = {
    
    }


const password = (props: passwordProps) => {
    return (
        <View>
            <Text>password component</Text>
        </View>
    )
}

export default password